# 進捗: 黒板補正さん（kokuban-adjust）

## 2026-08-28 セッション（初版）

### 技術選定

- **scanic v1.6.0（MIT）を採用**。四隅検出・透視変換・Corner Editor
  （タッチ操作・拡大ルーペ・キーボード操作）が実装済みで、用途にそのまま合う。
  ビルド済み ESM を `lib/` にベンダリングし、ビルド工程なしで読み込む。
- **jscanify は不採用**。OpenCV.js 依存で `node_modules` 実測 30MB
  （scanic は 3 ファイル計 172KB）。学校の回線でスマートフォンから開く用途には
  重すぎる。Corner Editor とルーペも自前実装が必要になる。
  比較用に `lab.html` から CDN 経由で読み込めるようにはしてある。
- **Nitidoc は不採用（AGPL-3.0）**。コードは流用せず、UX の参考のみ。
- scanic のバンドルを読んで確認した重要点: `extractDocument` は渡された画像を
  原寸のまま読み、出力サイズを四隅の実ピクセル距離から決める。縮小は入らない。
  一方 `scanDocument` は縮小して検出し、四隅を原寸座標系へ復元して返す。
  要件どおりの処理がライブラリ側で成立していた。

### 実装

- 3 画面（写真を選ぶ → 四隅を合わせる → 保存する）の最小構成。
- `js/scanner-adapter.js` に scanic への依存を隔離。アプリ本体は
  `detectBoard` / `correctPerspective` / `createCornerEditorAdapter` のみ呼ぶ。
- EXIF Orientation は `createImageBitmap(file, {imageOrientation:'from-image'})`
  で適用（EXIF パーサは自作しない）。HEIC/HEIF はマジックナンバーで判定し、
  「設定 → カメラ → フォーマット」を案内する専用メッセージを出す。
- 保存は JPEG（品質 0.92 固定）と PNG（無劣化）。

### 検証結果（Chromium / iPhone 相当ビューポート）

合成の高解像度写真で計測。

| 入力               | 検出              | 補正  | 出力                  |
| ------------------ | ----------------- | ----- | --------------------- |
| 4032×3024 (12.2MP) | OK 0.915 / 372ms  | 511ms | 3557×2208             |
| 3024×4032 (縦)     | OK 0.918 / 278ms  | 461ms | 2457×2688             |
| 7303×5477 (40MP)   | OK 0.922 / 1.4s   | 4.2s  | 5121×3261（上限適用） |
| 低コントラストの壁 | 検出失敗 → 手動へ | —     | —                     |

出力が入力より小さいのは黒板が写真の一部を占めるためで、不要な縮小はない。
12MP を 6 回連続処理して JS heap は 93MB で横ばい（リークなし）。

### 検証で見つけて直した問題

- **壁と板面の色差が小さい写真で、確度 0.155 の細長い四角形（2579×32 px）を
  「検出成功」として返していた。** そのまま補正すると帯状の画像になる。
  黒板は写真の大部分を占め極端に細長くはならない、という用途固有の前提で弾く
  `isPlausibleBoard()` を追加（確度・面積比・辺長比）。現在は正しく検出失敗と
  判定され、手動 4 点指定へ倒れる。
- 編集画面で画像が縦にレターボックスされ操作領域が狭かった。表示領域を画像の
  アスペクト比に合わせて解消。

### 残課題

- **ML detector（`detector: 'ml'`）が未検証。** 開発環境から jsDelivr へ接続
  できずモデルを取得できなかった。アプリ本体は classical のみを使う設定。
  実機で確認し、有効なら `scanic-ml` を同梱して自己ホストしたい
  （学校の回線で CDN に依存しないため）。
- 出力の縦横比は四隅の対辺長から決まるため、実際の黒板の比（例 4:1）とは
  一致しない。強い斜め撮影ほどずれる。縦横比を指定する UI は次の候補。
- 40MP の補正に約 4 秒。Web Worker 化は未実施（12MP では 0.5 秒）。
- 画質補正（Whiteboard / Blackboard モード）は未着手。まず幾何補正を優先。

### 関連

- 組織ハブ: https://edi-tool.github.io/ （`edi-tool/edi-tool.github.io` リポジトリ）
- **ハブページと `sitemap.xml` への本ツール追加は未実施**（別リポジトリのため）。

## 2026-08-28 セッション（公開後の 404 調査 + バグ修正）

### 404 の原因

公開 URL (https://edi-tool.github.io/kokuban-adjust/) が
`404 File not found` になる問題を調査。GitHub Actions の `page_build`
イベント実行回数を確認したところ **0 件**で、GitHub Pages が
リポジトリ側で一度も有効化されていないことを確認した
（有効化した瞬間に Jekyll の初回ビルドが走り `page_build` イベントが記録される）。

これはコードの問題ではなく、リポジトリの Settings → Pages でのブランチ選択
（`main` / `/(root)`）が未実施なだけ。Settings の変更権限を持つツールがないため、
**リポジトリ管理者による一度きりの手動設定が必要**。有効化手順は README に追記した。

### 見つけて直したバグ

- **`readjustBtn`（四隅を再調整）が常に「自動検出しました」の案内文を表示していた。**
  手動 4 点指定で確定した写真を補正 → 結果画面 → 再調整、と進んだ場合でも
  `openEditor(true)` を固定で呼んでいたため、実際は手動指定だったのに
  自動検出成功したかのような文言が出ていた。
  `state.detected` に検出成否を保持し、再調整時もそれを引き継ぐよう修正
  （`js/app.js`）。Chromium + iPhone 相当ビューポートで、自動成功／手動フォール
  バックの両経路を通し確認済み。PR #2 としてマージ済み。

### セッションクローズ時点の状態

- コード側は MVP として完成。PR #1（初版 + edi-tool 流への再構成）、
  PR #2（404 調査 + 再調整バグ修正）とも merge 済み。
- **GitHub Pages の有効化はこのセッションの外側の作業として依頼中。**
  この開発環境は `*.github.io` への outbound アクセスが遮断されているため、
  有効化後に実際に公開されたかは本セッションから確認できていない。
  次回セッションで確認する場合は、Actions タブの `pages build and deployment`
  履歴（`page_build` イベント）の有無、または実機ブラウザでの
  https://edi-tool.github.io/kokuban-adjust/ 表示確認で判断すること。
- 残課題（ML detector 実機検証、縦横比指定 UI、Web Worker 化、画質補正、
  ハブページ／sitemap.xml への追加）は上記のとおり未着手。次セッションは
  実画像での検証結果を受けての改善、またはハブページ追加から着手するのが
  自然な継続点。

## 2026-08-29 セッション（縦横比のズレへの対応）

### 問題

斜め撮影の写真を補正すると、出力の縦横比が実際の黒板とずれる報告があった。
原因は前回セッションの「残課題」に記載済みのとおり、透視補正の出力サイズが
四隅の対辺長（scanic の `extractDocument` 内部と `predictOutputSize` は同じ式）
から決まり、被写体の実際の縦横比とは無関係なこと。強い斜め撮影ほど対辺長の差が
大きくなり、ずれも大きくなる。バグというより、透視補正だけでは実寸を知りようが
ないという原理的な制約。

### 対応

結果画面に縦横比の指定 UI を追加（progress.md に元々あった「縦横比を指定する UI」
案を採用）。

- `js/app.js`: 透視補正の生の結果 (`state.result.canvas`) は不変のまま保持し、
  縦横比が指定されたときだけ、面積（総ピクセル数）を保ったまま指定比率に
  引き伸ばした別 canvas (`state.output`) を作って表示・保存に使う。非破壊。
  プリセット（4:3 / 3:4 / 16:9 / 9:16）とカスタム W:H 入力、「自動（検出のまま）」
  を用意。
- `index.html`: `.aspect-row` 以下に select とカスタム入力 UI を追加。他ツールと
  共通のデザイントークンに合わせた。

### 検証

Playwright（ヘッドレス Chromium）で、canvas 上に自作した斜め四角形の合成画像を
使い確認: 自動（1315×858）→ 4:3 指定（1227×920, 比 1.333…）→ カスタム 5:1
指定（2375×475, 比 5.0）→ 自動に戻す（1315×858 に一致）。面積はどのケースも
約 112.9 万 px 前後で保たれている。コンソールエラーなし。

## 2026-08-29 セッション（自動検出（四隅検出）の精度改善）

### 問題

自動検出（Scanic classical）が四隅を検出しても、実際の黒板の角からずれる
（精度が低い）という報告があった。

### 調査

学校の回線事情から CDN（jsDelivr / unpkg）へは今回の開発環境からも到達できず、
ML detector（`detector: 'ml'`）の検証は今回も不可能だった。また実写真も手元に
ないため、Playwright + headless Chromium 上で canvas に合成した疑似黒板写真
（4032×3024、壁のノイズ・チョーク文字・チョークトレイ・周辺減光つき、8 種類の
ランダムな斜め角度）を用意し、既知の四隅（正解値）とのユークリッド距離平均で
検出精度を数値化して比較した。

`js/scanner-adapter.js` の `detectBoard()` を経由した比較（本番と同じ経路）:

| `maxProcessingDimension` | 平均誤差 | 検出時間の目安 | 検出失敗 |
| ------------------------ | -------- | -------------- | -------- |
| 1024（変更前）           | 約 42px  | 約 240ms       | 0/8      |
| 1600（変更後）           | 約 27px  | 約 480ms       | 0/8      |
| 2048（参考）             | 約 21px  | 約 590ms       | 0/8      |
| 3024＝原寸（参考）       | 約 9px   | 約 1.3s        | 0/8      |

検出は縮小画像に対して行われ、四隅座標だけが原寸座標系へ復元される
（`correctPerspective` には常に原寸を渡す方針は変わらない）ため、この値を
上げても出力解像度・処理内容は変わらず、検出の位置精度だけが上がる。
原寸まで上げれば最も精度が高いが 1 秒を超え、モバイルでの体感が悪化する
リスクがあるため、精度と速度のバランスで 1600 を採用した。

### 対応

- `js/scanner-adapter.js`: `DETECT_MAX_DIMENSION` を 1024 → 1600 に変更し、
  export してコメントに上記の実測根拠を追記。
- `js/lab.js`: 検出比較ラボが本番と異なる解像度で比較してしまわないよう、
  ハードコードしていた `1024` を `scanner-adapter.js` から import した
  `DETECT_MAX_DIMENSION` に置き換え（jscanify 側の縮小もこれに合わせた）。

### 残課題

- 今回の検証は合成写真によるものであり、実際のチョークやマーカーの反射、
  逆光、ボケなど実写真特有の要因は再現できていない。実機・実写真での
  再検証が望ましい。
- ML detector は依然未検証（CDN 到達不可のため）。自己ホストできれば
  検証・比較の候補になる（`progress.md` 既存の残課題を参照）。

### セッションクローズ時点の状態

- 本日の 2 件（縦横比のズレへの対応、自動検出の精度改善）はそれぞれ
  PR #4・PR #5 としてマージ済み。別セッションが残していた PR #3
  （progress.md のセッションクローズ記録）も同時にマージした。
  3 件とも `progress.md` 末尾への追記が競合したため、古い順
  （#3 → #4 → #5）に 1 件ずつ ready 化・マージし、都度ブランチへ
  `main` を取り込んでコンフリクトを解消してから進めた。マージ後、
  オープンな PR は 0 件。
- 2026-08-28 セッション（初版）の残課題のうち「出力の縦横比が実際の
  黒板の比と一致しない」は本日の PR #4 で対応済み（結果画面に縦横比
  指定 UI を追加）。「ML detector 未検証」は本日も CDN 到達不可のため
  未解決のまま。
- 今回の変更（縦横比 UI・検出解像度の変更）はいずれも合成画像
  （Playwright + headless Chromium）での検証にとどまる。実機・実写真
  （斜め撮影、チョーク／マーカーの反射、逆光など）での確認は未実施。
  次セッションで実写真を使った再検証ができると、今回の
  `DETECT_MAX_DIMENSION = 1600` という選択の妥当性もより確かになる。
- 残課題（実写真での再検証、ML detector 実機検証、Web Worker 化、
  画質補正、ハブページ／sitemap.xml への追加）は未着手。

## 2026-08-29 セッション（入口と出口の改善）

幾何補正そのものは要件を満たしているので、体験の**両端**——写真を入れるところと
補正結果を持ち出すところ——だけを直した。高機能な写真加工は引き続き狙わない。

### 出口

- **保存を Web Share 経由にした（最重要）。** `<a download>` + blob URL だけだと
  iOS Safari ではカメラロールに保存されず、ファイル App に落ちるか単に開くだけで、
  「撮る → まっすぐにする → 写真アプリに戻す」が完了しなかった。
  `navigator.canShare({files})` が真なら `navigator.share({files})` を使い、
  共有シートの「画像を保存」で写真アプリへ入るようにした（`deliverBlob()`）。
  非対応環境は従来のダウンロードへフォールバック。**`AbortError` /
  `NotAllowedError`（シートを閉じただけ）はエラー表示にしない**——ここを取り違えると
  キャンセルのたびに赤い文字が出る。共有可の端末では主ボタンを「写真に保存」に変える。
- **結果画面のピンチ拡大を解禁。** `.stage { touch-action: none }` が結果表示にも
  掛かっており、チョーク文字が読めるか拡大して確かめる操作を潰していた。
  四隅ドラッグに必要なのは編集中だけなので `.stage--edit` に分離した。
- JPEG / PNG のラジオ 2 択を `<details>`（「保存形式を変える」）へ格納。既定の JPEG
  0.92 で足りるので主導線から外し、結果画面の選択肢を減らした。
- `.actions .btn { flex: 1 1 140px }` は 360px 幅で 3 ボタンが不規則に折り返し、
  主ボタンが中段に来ていた。主ボタン全幅 1 行 + 副次ボタン 1 行の構成に変更
  （`.actions-primary` / `.actions-sub`）。あわせて `100dvh` と
  `env(safe-area-inset-top)` を追加。

### 入口

- 「黒板をその場で撮る」（`capture="environment"`）を主導線に、既存の写真選択を
  副次に。input を 2 つに分け、`handleFile()` を共通の入口にした。
- 読み込み〜検出中はボタン文言が変わるだけだった（40MP で 1.5 秒以上）。
  開始画面に `#startBusy` を置き、既存の `.busy` / `.spinner` を使って
  「写真を読み込んでいます…」→「黒板の四隅をさがしています…」の 2 段階を表示。
- 保存成功後に「次の写真を補正する」を出す。連続で板書を撮るときのタップ数を減らす。

### ホーム画面追加

`manifest.json`（`start_url`/`scope` は baseurl があるので相対 `./`）と
`icon-192.png` / `icon-512.png`（既存 `favicon.ico` の中身が 512px PNG だったので
そこから生成）を追加。アイコンは円形で四隅が透明なため `purpose` は `any` のみ、
`maskable` は指定しない。Service Worker は入れていない。

### 検証（Chromium 1194 / iPhone 13 相当ビューポート、Playwright）

- 4032×3024 を読み込み → 検出成功 → 補正 3387×2540 → JPEG / PNG 両方の保存
- 低コントラストの平面 → 検出失敗 → 手動 4 点の案内（`note-warn`）→ 補正まで到達
- スピナーの 2 段階ラベルを両方確認
- 共有あり（保存成功 / キャンセル / share が投げてダウンロードへ倒れる）の 3 経路
  ——キャンセル時に `error-msg` にならないことを確認
- 結果ステージの `touch-action` が `auto`、編集ステージが `none`
- 390px 幅で主ボタンが単独 1 行・全幅、副次 2 つが同じ行
- 3 周の連続処理で JS heap 103 → 104 → 103 MB（`releaseAll` 有効、リークなし）
- JS エラー・console エラーなし

### main の縦横比 UI との統合

このブランチの作業中に main 側で縦横比指定 UI（PR #4）と検出解像度
1024→1600（PR #5）が入ったため、main をマージして解決した。

- `index.html` の結果画面が両方から変更されて衝突。**縦横比 UI は PR #4 が
  置いた位置のまま残し**、保存形式のラジオだけを `<details>` に格納する形で
  併合した（片方の設計判断を勝手に取り消さない）。
- `js/app.js` は自動マージされたが、main が保存対象を `state.result.canvas`
  から `state.output`（縦横比適用後の canvas）へ変えていた。`deliverBlob` が
  受け取る blob も `state.output` から作られることを実測で確認
  （16:9 指定 → 共有された画像が 3981×2239）。

### 残課題（今回スコープ外）

- 検出失敗時に四隅を「順にタップ」して置くモード（4 ドラッグ → 4 タップ）
- 40MP 補正の Web Worker 化（現状メインスレッドで約 4 秒）
- 画質補正（Whiteboard / Blackboard モード）
- Service Worker によるオフライン起動
- ML detector の実機検証（前回から継続）

### マージ後に見つけて直したバグ

main の縦横比 UI とこのブランチを統合した状態で結果画面を検証し、2 件見つけた。

- **極端な縦横比で canvas が壊れる。** `stretchToRatio` は面積を保つので総ピクセル
  数は増えないが、1 辺だけが伸びる。カスタムに 1000:0.1 を入れると 298547×30 px の
  canvas を作ろうとし、iOS Safari の 1 辺 16384 px を超えて**例外も出ないまま中身が
  空になる**（保存して初めて真っ白と分かる）。`MAX_CANVAS_DIMENSION = 16384` に
  収まるよう比を保って縮めるようにした。1000:0.1 で 16384×2、中身が描かれることを確認。
- **保存後に縦横比を変えると「保存しました」が残る。** 変更後の画像も保存済みだと
  誤解する。出力を作り直す `applyAspectSelection` で保存状態の表示と
  「次の写真」ボタンを消すようにした。

あわせて、カスタム比の数値入力を 1 文字ごとに数百万画素の再描画をしていたため
150ms のデバウンスを入れた（プルダウンは即時のまま）。

## 2026-08-29 セッション（入口の主従入れ替え・縦横比プリセットの横長シフト）

ユーザー指示 2 件への対応。ロジック変更なし、`index.html` のみ。

- **「撮影済みを使う方を上にする（その場で撮影の方をサブにする）」**: 開始画面の
  主ボタン（`.btn-cta`、全幅）を「撮影済みの写真から選ぶ」（`pickBtn`）に、
  副次ボタンを「黒板をその場で撮る」（`shootBtn`）に入れ替えた。id・input
  要素・`handleFile()` 呼び出しは変更なし（見た目の主従のみ）。
- **「縦横比、縦長は不要。横長のバリエーションを増やす」**: `aspectSelect` の
  プリセットから縦長 2 種（3:4 / 9:16）を削除し、横長を 4:3・16:9 に加えて
  3:2・16:10 を追加（計 4 種、すべて横長）。「自動」「カスタム…」は維持。
  `selectedRatio()`（`js/app.js`）は `"W:H"` 文字列を汎用的にパースするだけで
  値自体を検証しないため、JS 側の変更は不要だった。カスタム入力の既定値
  （4:3）はもともと横長で変更なし。

### 検証

`python -m http.server` でローカル起動し、開始画面でボタンの主従が入れ替わって
いること、結果画面のプルダウンが 自動/4:3/3:2/16:9/16:10/カスタム の順で
縦長オプションが無いことを目視確認。`npx prettier --check index.html` が
パスすることを確認。ロジックに変更がないため Playwright での機能再検証は
省略した。

## 2026-08-29 セッション（四隅編集の使いづらさ・縦横比の境界ドラッグ）

ユーザー指示 2 件への対応。

### 1. 四隅を選ぶときの虫眼鏡・指の位置が使いづらい

`js/scanner-adapter.js` の `createCornerEditorAdapter()` は scanic の
Corner Editor をそのまま使っており（自作しない方針）、触れる余地は渡す
オプションだけ。今回は 2 点だけ変更した。

- ルーペを大きく・高倍率に（`size: 128→150`, `zoom: 2.5→3.2`）。細い
  チョーク文字の角を見分けにくいという報告に対応。
- **nudges（選択中の角を上下左右ボタンで 1px / 10px 単位に微調整する
  scanic 標準機能）を有効化した（`enabled: false→true`）。** 指先だと
  ドラッグだけでは角そのものにピクセル単位で合わせにくい、という
  「指の位置が使いづらい」への直接的な対応。ドラッグ操作自体（速度依存の
  慣性・スムージング）は scanic 内部の実装で、公開オプションが無く
  変更できない（自作しない方針のため踏み込まない）。

Playwright で編集画面を開き、角を 1 つ選択すると `.scanic-nudges` に
ボタンが 8 個（上下左右 × 2 段階）出ることを確認した。

### 2. 補正後の縦横比を境界ドラッグで変えられるようにしたい

既存の縦横比 UI（プルダウン・カスタム W:H 入力、`stretchToRatio` で面積を
保ったまま引き伸ばす非破壊処理）はそのままに、**同じ処理へたどり着く
もう 1 つの入力手段**として、結果画像の上下左右の境界をドラッグする
ハンドルを追加した（ユーザーに確認: はみ出す部分は伸縮方式に統一、
既存 UI と同じロジックを使う）。

- `index.html`: `#resultStage` の中に `.edge-handle--{top,right,bottom,left}`
  を常設の子要素として追加（`aria-hidden`。既存のプルダウン/数値入力が
  アクセシブルな代替手段のため）。`renderResultStage()` が
  `stage.innerHTML = ""` で丸ごと消していたのをやめ、`canvas` 要素だけを
  差し替えるようにした（消えると困るので）。
- `js/app.js`: Pointer Events でハンドルをドラッグ。**ドラッグ中は
  実際の canvas を再描画せず**、`#resultStage` に `is-dragging` クラスを
  付けて CSS で canvas を枠いっぱいに引き伸ばして見た目だけ追従させる
  （数百万画素の再描画をフレームごとに行うと重いため）。指を離した瞬間に
  最終比率を `aspectW`/`aspectH`（カスタム欄）へ反映し、既存の
  `applyAspectSelection()` を 1 回だけ呼んで本物の `stretchToRatio` に
  差し替える。既存の縦横比 UI と状態が常に一致する（プルダウンは
  ドラッグ後「カスタム…」になる）。
  伸縮方向は開始時の辺の 25%〜400% にクランプし、極端な操作でも
  `MAX_CANVAS_DIMENSION` の保険（既存）が効く範囲に収まるようにした。

### 検証（Playwright, 390×844 ビューポート）

合成した斜め四角形の写真で、四隅編集 → 補正 → 結果画面まで進めて確認。

- 右の境界を +120px ドラッグ: ドラッグ中は `#resultStage` に `is-dragging`
  が付き `aspect-ratio` が追従（444/231.438 など）。指を離すと
  1736×1237 → 2031×1058（面積はほぼ保存）に実際に再描画され、
  プルダウンが「カスタム…」・数値欄が 19.2:10 になることを確認。
- 上端を大きく（900px）ドラッグ: 3468×619（25% クランプが効き、面積は
  ほぼ保存されたまま極端な横長になる）に破綻なく到達。
- 縦横比プルダウンを「自動」に戻すと 1736×1237（ドラッグ前の値）に
  正しく戻る。
- コンソールエラー・ページエラーなし。

### 残課題

- ドラッグ中の「速度依存スムージング」など四隅編集の核心的な操作感は
  scanic 内部実装のため、公開オプションの範囲でしか調整できない。
  依然として「使いづらい」と感じる場合は、scanic 側のオプション追加を
  upstream に issue として出すか、方針を変えて自前実装するかの判断が
  必要になる（現時点では前者を優先し、自作はしない）。
- 境界ドラッグは非破壊の伸縮方式のみ。トリミング（はみ出し部分を捨てる）
  方式は今回ユーザーに確認のうえ見送った。将来必要になれば別モードとして
  追加する形が自然。

## 2026-08-29 セッション（虫眼鏡のスマホ操作性の追加改善）

前セッションの続き。「虫眼鏡のスマホでの操作性や、虫眼鏡部分の表示をもっと
使いやすくしたい」への対応。scanic の Corner Editor は自作しない方針のため、
公開オプション（`js/scanner-adapter.js`）と、その外側で完全に自分たちの
管理下にある CSS（`index.html`）の 2 方向で詰めた。

### 公開オプションの追加調整（`js/scanner-adapter.js`）

- ルーペをさらに拡大（`size: 150→170`, `zoom: 3.2→3.5`）。
- `margin: 8→16`。指先とルーペの間隔が狭く、指や手にルーペ自体が
  重なって見えることがあった。
- `crosshairColor` を白（既定）からアクセントカラー（`#f28c06`）へ。
  白いホワイトボードでは白い十字線がほぼ見えなくなるため。
- `crosshairSize: 18→24`, `borderWidth: 2→3` で視認性を上げた。
- `handleHitArea` / `theme.handleHit` を `44→48`。Apple HIG の推奨下限
  （44pt）ちょうどだったのを Material Design 側の推奨（48dp）に寄せ、
  実際の指先の接地面によりゆとりを持たせた。

### CSS 側で直せた、地味だが効く問題（`index.html` `.stage--edit`）

scanic 自体には手を入れていないが、コンテナ側の CSS でスマホ実機特有の
「操作性の悪さ」の一因になり得るものが未対策だった。

- **iOS Safari の長押しコールアウト対策が無かった。** 画像を長押しすると
  「写真を保存」等のコールアウトが出てドラッグが中断されうる。
  `-webkit-touch-callout: none` を追加。
- **タップのたびに出る灰色のハイライト矩形が消されていなかった。**
  指を置いた瞬間に一瞬光るため、正確に置けたか分かりにくくなる。
  `-webkit-tap-highlight-color: transparent` を追加。
- 併せて `user-select: none` / `-webkit-user-select: none` で、ドラッグ中に
  意図せずテキスト選択状態になるのを防いだ。

### 検証（Playwright, 390×844 + `hasTouch: true`）

- 角のハンドルを pointerdown → 移動でドラッグ中のルーペ表示をスクリーン
  ショットで確認。サイズが大きくなり、オレンジの十字線が濃い黒板でも
  はっきり見えることを確認。
- `#editStage` の computed style で `--scanic-handle-hit: 48px` と
  `-webkit-tap-highlight-color: rgba(0, 0, 0, 0)` を確認。
  `-webkit-touch-callout` は Chromium 独自プロパティではないため
  headless Chromium では検証できず（実機 Safari でのみ効く CSS のため、
  設定自体の妥当性はコード上のレビューに留まる）。
- nudges ボタン（前セッションで有効化）が今回の変更後も問題なく表示・
  機能することを確認。

### 残課題

- ドラッグの速度依存スムージングなど、操作感の核心部分は依然として
  scanic 内部実装の範囲内。公開オプションで詰められる余地はほぼ使い切った
  ため、これ以上の改善には upstream への要望、または自前実装への方針転換の
  判断が必要（現時点では見送り）。
- `-webkit-touch-callout` 等 iOS 固有 CSS の実機検証は今回もできていない
  （開発環境に iOS 実機が無いため）。次回、実機での確認ができると良い。

## 2026-08-29 のまとめ

この日は複数セッション（本チャット以外も含む）にまたがって作業したため、
1 日分の変更を通しで追えるよう記録をまとめておく。詳細は各セッションの
見出しを参照。

| PR                                                        | 内容                                                | 担当         |
| --------------------------------------------------------- | --------------------------------------------------- | ------------ |
| [#4](https://github.com/edi-tool/kokuban-adjust/pull/4)   | 結果画面に縦横比の指定 UI を追加（伸縮方式）        | 別セッション |
| [#5](https://github.com/edi-tool/kokuban-adjust/pull/5)   | 四隅自動検出の検出解像度を 1024→1600 に変更         | 別セッション |
| [#7](https://github.com/edi-tool/kokuban-adjust/pull/7)   | 写真の入口・出口の改善（Web Share 保存など）        | 別セッション |
| [#8](https://github.com/edi-tool/kokuban-adjust/pull/8)   | 開始画面のボタン主従入れ替え、縦横比プリセット変更  | 別セッション |
| [#9](https://github.com/edi-tool/kokuban-adjust/pull/9)   | 四隅編集のルーペ・nudges 追加、縦横比の境界ドラッグ | 本チャット   |
| [#10](https://github.com/edi-tool/kokuban-adjust/pull/10) | 虫眼鏡のスマホでの操作性・表示をさらに改善          | 本チャット   |

（PR #3・#6 はコードの変更ではなく、前日セッションの `progress.md` クローズ
記録を merge しただけのもの。内容は上記「2026-08-28 セッション」各項目に
それぞれ反映済み。）

### この日時点の残課題（各セッションの「残課題」節を集約）

- ML detector（`detector: 'ml'`）が依然未検証（開発環境から CDN へ到達不可）。
- 40MP 補正の Web Worker 化は未着手（現状メインスレッドで約 4 秒）。
- 画質補正（Whiteboard / Blackboard モード）は未着手。
- Service Worker によるオフライン起動は未着手。
- 検出失敗時に四隅を「順にタップ」して置くモードは未着手。
- 四隅編集の速度依存スムージングは scanic 内部実装のため公開オプションでは
  これ以上詰められない（upstream への要望 or 自前実装への方針転換が必要）。
- 境界ドラッグは伸縮方式のみ（トリミング方式は今回見送り）。
- `-webkit-touch-callout` 等 iOS 固有 CSS の実機（Safari）検証が未実施。
- ハブページ（`edi-tool/edi-tool.github.io`）・`sitemap.xml` への本ツール
  追加は未実施。
